package com.johngoodstadt.memorize.models

open class PersonLocalSQL {

    fun updatePropertyLocal(property:String, value:String, UID:String){
        println("updatePropertyLocal ${property} ${value} ${String} ")
    }
}